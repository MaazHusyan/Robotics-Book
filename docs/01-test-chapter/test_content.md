# Test Chapter
This is a test chapter for verifying that metadata is properly transferred during the embedding process.

## Section 1
This section contains some sample content that will be chunked and embedded.

## Section 2
This is another section with different content to test the chunking functionality.

The key thing to verify is that the source_file and source_location metadata are preserved when the content is processed through the embedding pipeline.